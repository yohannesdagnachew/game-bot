import { SharedOptions, SignData, SignDataAsyncFn, SignDataSyncFn, Text } from './types.js';

export type SignOptions = SharedOptions;
/**
 * Signs specified init data.
 * @param data - init data to sign.
 * @param authDate - date, when this init data should be signed.
 * @param key - private key.
 * @param signData - function signing data.
 * @param options - additional options.
 * @returns Signed init data presented as query parameters.
 */
export declare function sign(data: SignData, key: Text, authDate: Date, signData: SignDataSyncFn, options?: SignOptions): string;
/**
 * Signs specified init data.
 * @param data - init data to sign.
 * @param authDate - date, when this init data should be signed.
 * @param key - private key.
 * @param signData - function signing data.
 * @param options - additional options.
 * @returns Signed init data presented as query parameters.
 */
export declare function sign(data: SignData, key: Text, authDate: Date, signData: SignDataAsyncFn, options?: SignOptions): Promise<string>;
